export interface Track {
   // track_id: string;
   // album_id: string;
   // title: string;
   // disc_number: number;
   // track_number: number;
   // duration: Date; // o string
   // is_explicit: boolean;
   // plays: number;
   // source_file: string;
}

export interface DBTrack {
   track_id: string;
   album_id: string;
   title: string;
   disc_number: number;
   track_number: number;
   duration: Date; // o string
   is_explicit: boolean;
   plays: number;
   source_file: string;
}
